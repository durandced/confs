
;; ;; GNUSERV

;; (when has-gnuserv
;;   (gnuserv-start)
;; ;  (global-set-key [(control x) (control c)] 'gnuserv-close-session)
;;   (add-hook 'gnuserv-visit-hook 'configure-frame)
;;   )

(provide 'my-gnuserv)
